{
  "name": "server",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "{projectRoot}",
  "projectType": "application",
  "targets": {
    "generate-build-metadata": {
      "executor": "nx:run-commands",
      "dependsOn": ["config-lib:build"],
      "inputs": ["production", "webappBuildMetadata"],
      "outputs": ["{projectRoot}/dist/version.json"],
      "options": {
        "commands": ["rimraf {projectRoot}/dist", "node {projectRoot}/bin/generateVersionFile.mts"],
        "parallel": false
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build", "generate-build-metadata", "telemetry-lib:build"],
      "inputs": ["production", "webappBuildMetadata"],
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "commands": ["tsc -p {projectRoot}/tsconfig.json", "node {projectRoot}/bin/copy_server_assets.js"],
        "parallel": false
      }
    },
    "build-dev": {
      "executor": "nx:run-commands",
      "dependsOn": ["config-lib:build", "generate-build-metadata", "telemetry-lib:build"],
      "inputs": ["production", "webappBuildMetadata"],
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "commands": ["tsc -p {projectRoot}/tsconfig.json", "node {projectRoot}/bin/copy_server_assets.js"],
        "parallel": false
      }
    },
    "package": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build", "build", {"projects": "webapp", "target": "build"}],
      "inputs": ["production", "webappBuildMetadata"],
      "outputs": ["{projectRoot}/dist/s3"],
      "options": {
        "commands": ["node bin/zip.js"],
        "parallel": false
      }
    },
    "package-public": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build", "build", {"projects": "webapp", "target": "build-production-public"}],
      "inputs": ["production", "webappBuildMetadata"],
      "outputs": ["{projectRoot}/dist/s3"],
      "options": {
        "commands": ["yarn verify:bundle:prod:public", "node bin/zip.js"],
        "parallel": false
      }
    },
    "serve": {
      "continuous": true,
      "dependsOn": ["config-lib:build", "webapp:build-libs", "webapp:watch-deps-dynamic"],
      "executor": "@nx/js:node",
      "options": {
        "buildTarget": "server:build-dev",
        "buildTargetOptions": {
          "outputPath": "{projectRoot}/dist",
          "outputFileName": "index.js"
        },
        "watch": true
      },
      "configurations": {
        "production": {
          "buildTarget": "server:build"
        }
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/apps/server"],
      "options": {
        "jestConfig": "{projectRoot}/jest.config.js",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "coverage": true,
          "coverageReporters": ["html", "lcov", "text"]
        }
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint --config eslint.config.ts --cache --cache-location node_modules/.cache/eslint/server --max-warnings=1289 --ext .js,.ts {projectRoot}"
      }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "dependsOn": ["config-lib:build"],
      "options": {
        "command": "tsc --project {projectRoot}/tsconfig.json --noEmit"
      }
    }
  },
  "tags": ["type:app", "scope:backend"]
}
