name: Label PR

on:
  pull_request:
    types:
      - opened

jobs:
  # Automatically labels based on PR title
  label:
    name: Label PR
    runs-on: ubuntu-latest
    steps:
      - uses: srvaroa/labeler@v0.3
        env:
          GITHUB_TOKEN: ${{secrets.GH_TOKEN }}
