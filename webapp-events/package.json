{
  "description": "Events shared between Wire's web app and desktop app.",
  "devDependencies": {
    "rimraf": "3.0.2",
    "typescript": "4.8.3"
  },
  "files": [
    "src/**/*.js*",
    "src/**/*.d.ts"
  ],
  "license": "GPL-3.0",
  "main": "src/index",
  "name": "@wireapp/webapp-events",
  "repository": "https://github.com/wireapp/wire-web-packages/tree/main/packages/webapp-events",
  "scripts": {
    "build": "tsc",
    "build:node": "tsc",
    "clean": "rimraf \"src/*{.js,.js.map,.d.ts}\"",
    "dist": "yarn clean && yarn build",
    "test": "exit 0"
  },
  "version": "0.14.6"
}
