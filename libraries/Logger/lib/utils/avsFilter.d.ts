/**
 * Check if AVS log message is allowed
 * AVS logs are very verbose, so we only allow specific ones
 */
export declare function isAllowedAVSLog(message: string): boolean;
//# sourceMappingURL=avsFilter.d.ts.map
